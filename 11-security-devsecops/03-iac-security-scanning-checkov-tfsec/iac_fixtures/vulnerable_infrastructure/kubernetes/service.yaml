apiVersion: v1
kind: Service
metadata:
  name: insecure-api-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: insecure-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
