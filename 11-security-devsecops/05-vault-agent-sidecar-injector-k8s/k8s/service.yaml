apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: default
  labels:
    app.kubernetes.io/name: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
