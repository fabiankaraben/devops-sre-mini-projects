apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: time-skew-experiment
  namespace: chaos-lab
spec:
  mode: one
  selector:
    namespaces:
      - chaos-lab
    labelSelectors:
      app: payment-api
  timeOffset: '1h'
  duration: '25s'
