# ==============================================================================
# Network Port Scanner & Troubleshooter - Targets Manifest
# ==============================================================================
# Supported Formats:
#   <ip_address>           e.g. 127.0.0.1, 172.28.0.10
#   <hostname>             e.g. localhost, api.local
#   <cidr_subnet>          e.g. 172.28.0.0/28 (scans all usable host IPs)
#
# Blank lines and lines starting with '#' are ignored.
# ==============================================================================

# Localhost Endpoints (Tested against host mapped ports)
127.0.0.1
localhost

# Container Bridge Network Static IPs (Used when scanning from container network)
# 172.28.0.10
# 172.28.0.20
# 172.28.0.30

# Subnet CIDR Block Range Example
# 172.28.0.0/28

# Reference Public Targets (Uncomment to test DNS & external endpoints)
# one.one.one.one
# google.com
